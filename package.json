{
	"name": "univ",
	"version": "0.0.1",
	"scripts": {
		"dev": "vite dev  --host",
		"build": "vite build",
		"preview": "vite preview",
		"test": "vite preview --port 80",
		"prod": "vite build && vite preview --port 80 --host",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch"
	},
	"devDependencies": {
		"@sveltejs/adapter-static": "^3.0.1",
		"@sveltejs/kit": "^2.12.2",
		"@sveltejs/vite-plugin-svelte": "^3.0.2",
		"svelte": "^4.2.19"
	},
	"type": "module",
	"dependencies": {
		"hammerjs": "^2.0.8",
		"maplibre-gl": "^4.0.1",
		"salles_module": "github:beaulieu-camp/salles_module",
		"vite": "^5.1.7"
	}
}
