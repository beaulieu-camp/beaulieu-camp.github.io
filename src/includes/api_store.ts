import { writable, type Writable } from 'svelte/store';

var weatherData:Writable<values> = writable([])
var starData:Writable<values> = writable([])
var buData:Writable<values> = writable([])
var sallesData:Writable<values> = writable([])
var restoData:Writable<values> = writable([])